# go-container

Container runtime, LinuxKit builder, and dev environment management.
Supports LinuxKit (QEMU/Hyperkit), Apple Containers (macOS 26+), and TIM (experimental).

**Module:** `dappco.re/go/container` — see [docs/](docs/) for full documentation.
