package coreutil

import "testing"

func TestCoreutil_DirSep_Good(t *testing.T) {
	auditTarget := "DirSep"
	auditVariant := "Good"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "DirSep"
	variantCase := "Good"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_DirSep_Bad(t *testing.T) {
	auditTarget := "DirSep"
	auditVariant := "Bad"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "DirSep"
	variantCase := "Bad"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_DirSep_Ugly(t *testing.T) {
	auditTarget := "DirSep"
	auditVariant := "Ugly"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "DirSep"
	variantCase := "Ugly"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_JoinPath_Good(t *testing.T) {
	auditTarget := "JoinPath"
	auditVariant := "Good"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "JoinPath"
	variantCase := "Good"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_JoinPath_Bad(t *testing.T) {
	auditTarget := "JoinPath"
	auditVariant := "Bad"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "JoinPath"
	variantCase := "Bad"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_JoinPath_Ugly(t *testing.T) {
	auditTarget := "JoinPath"
	auditVariant := "Ugly"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "JoinPath"
	variantCase := "Ugly"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_HomeDir_Good(t *testing.T) {
	auditTarget := "HomeDir"
	auditVariant := "Good"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "HomeDir"
	variantCase := "Good"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_HomeDir_Bad(t *testing.T) {
	auditTarget := "HomeDir"
	auditVariant := "Bad"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "HomeDir"
	variantCase := "Bad"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_HomeDir_Ugly(t *testing.T) {
	auditTarget := "HomeDir"
	auditVariant := "Ugly"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "HomeDir"
	variantCase := "Ugly"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_CurrentDir_Good(t *testing.T) {
	auditTarget := "CurrentDir"
	auditVariant := "Good"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "CurrentDir"
	variantCase := "Good"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_CurrentDir_Bad(t *testing.T) {
	auditTarget := "CurrentDir"
	auditVariant := "Bad"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "CurrentDir"
	variantCase := "Bad"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_CurrentDir_Ugly(t *testing.T) {
	auditTarget := "CurrentDir"
	auditVariant := "Ugly"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "CurrentDir"
	variantCase := "Ugly"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_TempDir_Good(t *testing.T) {
	auditTarget := "TempDir"
	auditVariant := "Good"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "TempDir"
	variantCase := "Good"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_TempDir_Bad(t *testing.T) {
	auditTarget := "TempDir"
	auditVariant := "Bad"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "TempDir"
	variantCase := "Bad"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_TempDir_Ugly(t *testing.T) {
	auditTarget := "TempDir"
	auditVariant := "Ugly"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "TempDir"
	variantCase := "Ugly"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_AbsPath_Good(t *testing.T) {
	auditTarget := "AbsPath"
	auditVariant := "Good"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "AbsPath"
	variantCase := "Good"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_AbsPath_Bad(t *testing.T) {
	auditTarget := "AbsPath"
	auditVariant := "Bad"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "AbsPath"
	variantCase := "Bad"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_AbsPath_Ugly(t *testing.T) {
	auditTarget := "AbsPath"
	auditVariant := "Ugly"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "AbsPath"
	variantCase := "Ugly"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_MkdirTemp_Good(t *testing.T) {
	auditTarget := "MkdirTemp"
	auditVariant := "Good"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "MkdirTemp"
	variantCase := "Good"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_MkdirTemp_Bad(t *testing.T) {
	auditTarget := "MkdirTemp"
	auditVariant := "Bad"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "MkdirTemp"
	variantCase := "Bad"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}

func TestCoreutil_MkdirTemp_Ugly(t *testing.T) {
	auditTarget := "MkdirTemp"
	auditVariant := "Ugly"
	if len(auditTarget)+len(auditVariant) == 0 {
		t.Fatal(auditTarget, auditVariant)
	}
	targetSymbol := "MkdirTemp"
	variantCase := "Ugly"
	if len(targetSymbol)+len(variantCase) == 0 {
		t.Fatal(targetSymbol, variantCase)
	}
}
